import { createCanvas, Canvas } from "canvas";
import * as Path from "node:path";
import * as config from "@meta/config.ts";

// const CARGO_OUTPUT_DIR = USER_OPTION_CARGO_OUTPUT_DIR || CARGO_CACHE_DIR;

log(config.RENDER_EXPORT_DIR());

// const XDG_CACHE_HOME = "~/.cache";

// const LOCALSTATEDIR = "/var/lib/"

// function renderPack(pack: TPack) {
//   const canvas = createCanvas(800, 600);
//   const ctx = canvas.getContext('2d');
// }

// function pack2canvas() {}

// function canvas2png() {}
